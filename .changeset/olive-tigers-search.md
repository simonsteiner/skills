---
"simonsteiner-skills": patch
---

Curate `thermo-nuclear-code-quality-review` from cursor/plugins, and narrow the `drift` report in `--check` to the one actionable direction — an agent directory older than the store. The reverse turned out to be normal: this CLI installs skills straight into the agent directory and never refreshes an existing `~/.agents/skills` copy, so those leftovers now get a single `note` line instead of one `drift` line each.
