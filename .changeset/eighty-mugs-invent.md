---
"simonsteiner-skills": minor
---

Add two engineering skills. `address-review-findings` pulls a PR's unresolved review threads via `gh`, triages each against the code, applies the fixes, and replies and resolves only with an explicit go-ahead. `sync-and-branch` fast-forwards the default branch and cuts a feature branch from it, refusing to run on a dirty worktree rather than stashing anything.
