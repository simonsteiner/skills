# Skills

A collection of agent skills (slash commands and behaviors) loaded by Claude Code. Currently the repo ships a single skill, `/conventional-commit`, which writes Conventional Commits messages from the current worktree state.
